# pnpm/npm configuration for Blakor

# Sharp optimization - only build for current platform
sharp-install-skip=false

# Ignore platform-specific optional dependencies failures
optional=true

# Auto-install peer dependencies
auto-install-peers=true

# Strict peer dependencies off for compatibility
strict-peer-dependencies=false
